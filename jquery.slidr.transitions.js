// Transitions for the slider
$.slidr_transitions = function(plugin, options) {
  
};

// Before transition
$.slidr_transitions.prototype.before = function() {
  
}
// After transition
$.slidr_transitions.prototype.after = function() {
  
}